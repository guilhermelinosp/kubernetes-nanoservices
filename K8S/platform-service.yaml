apiVersion: v1
kind: Service
metadata:
  name: platform-service
spec:
  selector:
    app: platform-deployment
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
    - name: grpc
      protocol: TCP
      port: 666
      targetPort: 666
  type: ClusterIP
